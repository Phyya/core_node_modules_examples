/*
Name : Console module
Description: The console module provides a simple debugging console that is similar to the JavaScript console mechanism provided by web browsers. The console module can be used to print debug information to the console, such as variable values, error messages, or stack traces.

*/

const x = 10;
const y = 20;
const z = x + y;

console.log(`The sum of ${x} and ${y} is ${z}`);
